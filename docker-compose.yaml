version: '3.6'
services:
    api:
        container_name: 'rajasinga_api'
        build: ./server
        ports:
            - '3001'
        environment:
            - PORT=3001
            - MODE=development
        volumes:
            - 'rajadir:/server'
        networks:
            - raja_network
    client:
        container_name: 'rajasinga_client'
        build: ./client
        volumes:
            - rajadir:/client
        ports:
            - '3002'
        environment:
            - mode=development
            - PORT=3002
        networks: 
            - raja_network
    proxy:
        container_name: 'rajasinga_proxy'
        build: ./proxy
        volumes:
             - rajadir:/proxy
        networks:
            - raja_network
        links:
            - api
            - client

        ports:
            - '80:80'
            - '8080:8080'

networks:
    raja_network:
        driver: bridge
volumes:
    rajadir:
